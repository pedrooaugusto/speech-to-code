{
  "name": "speech2code.spoken",
  "version": "0.0.1",
  "description": "Transforming natural language in code",
  "main": "dist/index.js",
  "scripts": {
    "test": "tsc && jest dist/__tests__/(.*).js$",
    "build": "rm -rf dist/* && tsc && node dist/build.js && rm -rf dist/build.js",
    "build-docs": "node src/build-docs.js"
  },
  "keywords": [
    "Speech-to-code"
  ],
  "dependencies": {
    "@microsoft/recognizers-text-number": "^1.3.0"
  },
  "author": "Pedro Silva",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@hpcc-js/wasm": "^1.4.1",
    "@types/graphlib": "^2.1.7",
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.35",
    "graphlib-dot": "^0.6.4",
    "jest": "^26.6.3",
    "svg2img": "^0.9.1"
  }
}
